# Samples

Place sample invoices, receipts, and contracts here.  The default scripts expect:

- `samples/invoice/*.png` or `.pdf` for quick Donut/VL smoke tests
- `samples/invoice/example.png` referenced by `opensource_processor.py`

Use your own test files or copy anonymized invoices into `samples/invoice/`.
